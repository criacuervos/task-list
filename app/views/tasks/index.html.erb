<h1>all of the shit i have to do!!!!!</h1>

<section>
  <h2>here are your tasks:</h2>
  <ul>
    <% @tasks.each do |task| %>
    <li>
      <%= link_to task.name, task_path( task.id ) %>:
      <%= task.description %>
    </li>
    <% end %>
  </ul>
</section>
